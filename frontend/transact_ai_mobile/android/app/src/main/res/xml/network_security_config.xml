<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows cleartext HTTP traffic to your local Python backend (10.0.2.2).
  For production, switch to HTTPS and remove the domain entry.
  Place this file at:
    android/app/src/main/res/xml/network_security_config.xml
-->
<network-security-config>
  <domain-config cleartextTrafficPermitted="true">
    <!-- Android emulator → your machine -->
    <domain includeSubdomains="false">10.0.2.2</domain>
    <!-- Physical device on LAN -->
    <domain includeSubdomains="false">192.168.1.0</domain>
    <domain includeSubdomains="false">localhost</domain>
    <domain includeSubdomains="false">127.0.0.1</domain>
  </domain-config>
</network-security-config>